/*
This interface defines the props for the Hero component.
The Hero component is a reusable UI element used for displaying hero sections
on webpages. It may include an optional logo (hasLogo prop) to be displayed 
alongside the hero content.
*/
export interface IHeroProps {
	hasLogo?: boolean;
}
